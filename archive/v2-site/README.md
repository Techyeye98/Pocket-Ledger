# Archived: former V2 homepage

`index.html` here is the **frozen V2 marketing homepage** that previously lived at
`distribution/index.html` (GitHub Pages root). It was replaced by the V3 site on
2026-09-18.

- It is **not deployed** — GitHub Pages publishes `distribution/` only.
- It is kept verbatim so the previous design, copy and asset links stay inspectable.
- Its relative links (`downloads/…`, `guides/…`) are preserved as written; they are
  no longer rooted correctly for this folder.

Git history also holds it: commit `d337f26` ("Make Pocket Ledger V2 the root GitHub
Pages homepage").
